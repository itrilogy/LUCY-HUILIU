# VectorStream 矢量流 - 软件用户使用手册

**软件名称**: VectorStream 矢量流本地素材管理系统  
**软件版本**: v1.4.0 (Concurrent & Scalable)
**开发/著作权人**: Kwangwa Hung / 鹿溪联合创新实验室

---

## 第一章：软件概述

### 1.1 软件简介
VectorStream 是一款基于人工智能（AI）驱动的本地 SVG 矢量素材智能管理系统。针对设计团队、开发者及内容创作者面临的“素材查找难、管理混乱、版权不清”等痛点，本系统利用大语言模型（LLM）的视觉理解能力，对海量本地 SVG 文件进行自动化语义标注与分类，实现了“所想即所得”的智能检索体验。

### 1.2 主要功能特性
1.  **AI 语义检索**: 集成视觉大模型，支持中英文双语、多关键词组合及隐式分类检索。
2.  **实时个性化定制**: 支持全局主题色一键替换，实时预览并下载带色素材。
3.  **智能分类体系**: 内置基于 8 大核心场景（商业、科技、教育等）的科学分类树。
4.  **高性能浏览**: 采用现代化 Glassmorphism（毛玻璃）UI，支持动态网格布局与懒加载技术，流畅管理数千个矢量文件。
5.  **双格式输出**: 提供 SVG 源码与 PNG 高清位图两种导出格式，满足不同使用场景。

---

## 第二章：安装与部署

### 2.1 运行环境要求
- **操作系统**: Windows 10/11, macOS, Linux
- **依赖环境**: 
    - Python 3.8 或更高版本（用于后台数据处理）
    - Node.js (推荐 v14+，用于启动本地静态服务)

### 2.2 安装步骤
1.  **获取源码**: 将软件压缩包解压或通过 Git 克隆至本地目录 `VectorStream`。
2.  **安装依赖**:
    打开终端（Terminal/CMD），进入项目根目录：
    ```bash
    cd VectorStream
    pip install requests  # 安装 AI 模块依赖
    ```

### 2.3 启动软件
1.  在终端中执行以下命令启动服务：
    ```bash
    npx serve . -l 3001
    ```
2.  打开浏览器访问地址：`http://localhost:3001` 即可进入系统主界面。

---

## 第三章：功能使用说明

### 3.1 界面概览
系统主界面包含以下核心区域：
- **顶部导航栏**: 包含品牌标识、全局搜索框、结果统计数及视图控制区。
- **分类导航条**: 横向排列的胶囊状分类标签（如：商务办公、科技数字化...）。
- **素材展示区**: 瀑布流/网格形式展示的 SVG 素材卡片。
- **分页控制区**: 底部的翻页按钮与页码指示器。

### 3.2 浏览与筛选
- **分类切换**: 点击分类导航条上的标签（例如“科技数字化”），素材列表将仅展示该分类下的内容。点击 "全部 (All)" 可恢复默认视图。
- **视图调整**:
    - **网格密度**: 拖动顶部的滑块，可将每行显示的素材数量在 2 列至 10 列之间无级调节。
    - **每页行数**: 通过下拉菜单选择每页显示的行数（5行 / 10行 / 全部），适应不同屏幕高度。

### 3.3 智能搜索
系统搜索框支持多种高级检索模式：
- **关键词搜索**: 输入文件名关键词（如 `server`）或中文描述（如 `服务器`）。
- **多词组合 (AND)**: 输入空格分隔的多个词（如 `business blue`），系统将筛选出同时满足“商务场景”且具有“蓝色元素”的素材。
- **隐式分类**: 直接输入中文分类名（如 `医疗`），系统会自动在该分类下进行检索，无需手动切换标签。

### 3.4 个性化定制与下载
1.  **主题色修改**: 点击顶部的圆形颜色选择器，选择任意颜色（如企业品牌色）。界面中所有素材的主色调将实时变更为选定颜色。
2.  **预览与下载**:
    - 点击任意素材卡片，弹出高清预览详情页。
    - **下载 SVG**: 获取包含当前配色的矢量源码文件，适合设计师二次编辑或开发使用。
    - **下载 PNG**: 系统实时渲染当前配色的高清位图，适合直接插入 PPT 或文档中。

### 3.5 素材批量导入与整合（进阶功能）
系统支持将外部 SVG 素材批量导入并重命名，以确保文件名的规范性与唯一性。

1.  **执行导入**: 
    您可以从任意本地目录导入素材：
    ```bash
    # 从自定义路径导入示例
    python3 process_icons.py --source /你的/素材/文件夹路径
    
    # 默认从 assets/ICON 导入
    python3 process_icons.py
    ```
2.  **生成清单**: 脚本会自动将文件重命名（格式：`来源-分类-名称.svg`）并复制至 `assets/illustrations` 目录，同时生成 `incremental_task.json` 任务清单。

### 3.6 AI 标注管理（后台功能）
若需对新导入的素材执行 AI 语义分析，请运行：
```bash
# 执行并发标注（推荐）
python3 ai_tagger.py --workers 10
```
系统将自动分析素材，生成标题、描述及分类标签，并同步更新索引数据库。

---

## 第四章：版权与致谢

- **开源协议**: 本软件遵循 MIT 开源许可协议。
- **素材来源**: 系统内置素材由以下开源项目提供，均支持免费商用（含个人及商业用途）：
    - **矢量插画**: unDraw, Lukasz Adam, VectorCraftr。
    - **通用图标**: IconPark (字节跳动), Lucide, Tabler Icons, Feather, Heroicons, Iconoir 等。
- **技术支持**: 由鹿溪联合创新实验室提供技术维护。

---
**文档版本**: 1.4 | **更新日期**: 2026-02-25
