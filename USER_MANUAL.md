# 绘流 · HuiLiu（VectorStream）  
# 软件用户使用手册

**软件名称**：绘流 · HuiLiu（VectorStream）本地矢量素材管理系统  
**软件版本**：V1.8.0  
**开发 / 著作权人**：Kwangwa Hung  
**出品**：鹿溪联合创新实验室（LUXI Joint Innovation Lab）  
**产品标语**：引线定锚，聚迹成流  
**配套说明书**：`docs/软件说明书.md`（软著鉴别材料）

---

## 第一章　软件概述

### 1.1 简介

绘流 · HuiLiu（VectorStream）是一款在本机运行的 SVG 矢量素材管理系统。它把浏览、语义检索、预览去背 / 换色、选块编辑和双格式导出放在同一界面，并可选择用视觉大模型为新素材补标签。默认离线可用，不依赖外网完成检索与下载。

### 1.2 用户角色

| 角色 | 能力 |
| :--- | :--- |
| 普通用户 | 浏览、搜索、预览编辑、下载当前 SVG / PNG |
| 管理员 | 上述全部，外加将当前预览写回 `assets/illustrations` |

两者共用同一套页面，不是两套产品。管理员口令只写在服务器侧 `.env`，不会出现在网页源码里。

### 1.3 主要功能

1. AI 语义检索（标题、描述、分类、标签；多词 AND；分类名隐式命中）  
2. 网格浏览、虚拟列表、大图位图预览  
3. 预览编辑：按色清除、整图换色、擦除选块、填充选块、去掉画板框  
4. 下载当前预览（不必先保存）  
5. 深色模式与键盘浏览  
6. 批量导入、标签清洗、精简目录重建  
7. 管理员登录与素材库写回  

---

## 第二章　安装与启动

### 2.1 运行环境

- 操作系统：Windows 10/11、macOS、Linux  
- Python 3.8 或更高版本（推荐用自带 `server.py`）  
- 现代浏览器（Chrome / Edge / Safari / Firefox）  
- Node.js 仅在使用 `npx serve` 时需要  

### 2.2 安装

```bash
cd VectorStream
pip install requests    # 仅 AI 标注需要
cp .env.example .env    # 若需要管理员写回，再编辑填写 VS_ADMIN_PIN
```

### 2.3 启动

推荐（支持 gzip、管理登录、写回）：

```bash
python3 server.py --port 3001
```

浏览器打开 `http://127.0.0.1:3001`。

仅浏览、不登录时也可用：

```bash
npx serve . -l 3001
```

此时管理登录不可用。

若 3001 提示 Address already in use，先结束占用该端口的旧进程再启动。

---

## 第三章　日常使用

### 3.1 界面

- **顶栏**：产品标、搜索框、每页行数、网格密度、全局主色、深色模式、**管理登录**  
- **分类条**：商务办公、科技数字化等  
- **网格**：等比例卡片，棋盘格衬底显示透明  
- **页脚**：实验室出品标、管理登录  

点击左上角 Logo 打开关于与版权声明；出品区并列显示应用标与实验室主标。

### 3.2 浏览

- 点分类胶囊筛选；点「全部素材」恢复。  
- 网格密度 2–10 列；每页行数 5 / 10 / 20 / 40 / 无分页。  
- 「无分页」只渲染可见行，可继续滚动。  
- 方向键移动焦点，Enter 打开预览。  

### 3.3 搜索

在顶栏输入关键词。支持：

- 文件名或中文描述（如 `server`、`服务器`）  
- 空格分隔的多词 AND（如 `business blue`）  
- 直接输入分类名（如 `医疗`）做隐式分类检索  

搜索框已关闭浏览器自动填充：这不是业务「搜索历史」，而是浏览器按输入框名称记住的旧值。本软件不保存检索记录到服务器。

### 3.4 预览编辑

点击卡片打开弹窗。左侧为棋盘格预览，右侧为编辑与导出。

**取色**  
点「背景色」或「主配色」所在行，再点预览图形，光标处颜色写入该色板；也可打开系统色板取色。

**整图**  

| 按钮 | 作用 |
| :--- | :--- |
| 按色清除 | 去掉图中所有接近取样色的填色 / 描边 |
| 整图换色 | 把图中该颜色全部映射为全局主色 |
| 去掉画板框 | 只删除铺满画板的矩形框，不按颜色、不动画面里的色块 |

**选块（开关，可再点关闭）**  

| 按钮 | 作用 |
| :--- | :--- |
| 擦除选块 | 只擦掉点中的那一块闭合图形 |
| 填充选块 | 只用当前主配色填充点中的那一块 |

Esc：先退出选块模式，再按关闭弹窗。撤销 / 还原可回退编辑栈。

下载始终导出**当前预览**，不必先保存。有未保存修改时文件名带 `-edited`。

| 按钮 | 作用 |
| :--- | :--- |
| 下载当前 SVG | 当前画面的矢量文件 |
| 下载当前 PNG | 按比例导出的透明位图 |
| 写回素材库 | 仅管理员可见，覆盖仓库中对应 SVG |

### 3.5 主题

顶栏圆形色盘改全局主色；月亮按钮切换深色模式，偏好记在本机。

### 3.6 管理员

1. 顶栏或页脚点击 **管理登录**。  
2. 在关于弹窗中输入 `.env` 里的 `VS_ADMIN_PIN`。  
3. 登录成功后按钮变为「管理员」，预览弹窗出现「写回素材库」。  
4. 「退出管理」结束会话。  

口令只由 `server.py` 读取并校验，页面源码中没有口令。未配置 `.env` 或未用 `server.py` 启动时，登录区会提示原因。

---

## 第四章　进阶：导入、标注与目录

### 4.1 批量导入

```bash
python3 process_icons.py --source /你的/素材/文件夹
```

文件重命名为 `来源-分类-名称.svg`，复制到 `assets/illustrations`，并生成 `incremental_task.json`。

### 4.2 AI 标注

```bash
python3 ai_tagger.py --workers 10
```

需在脚本中配置可用的视觉模型接口。结果写入 `metadata.json` 与 `ai_cache.json`。

### 4.3 标签清洗与重建目录

```bash
python3 clean_tags.py    # 去掉残片、描述句，合并近义标签
python3 build_index.py   # 生成 assets/catalog.json 供首屏检索
```

---

## 第五章　版权与声明

- 程序部分：MIT License。  
- 内置素材来自开放版权项目，须遵守各自许可：  
  - 插画：unDraw、Lukasz Adam、VectorCraftr  
  - 图标：IconPark、Lucide、Tabler Icons、Feather、Heroicons、Iconoir 等  
- 出品与维护：鹿溪联合创新实验室  

---

**文档版本**：1.8　**更新日期**：2026-08-13  
本手册与 `docs/软件说明书.md` 同步，作为软著登记之使用说明书。
